<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' | Admin' : 'Admin Panel' %> | Webjinny</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
<div class="admin-layout">
  <!-- Sidebar -->
  <aside class="admin-sidebar">
    <div class="sidebar-logo"><span>Webjinny</span> Admin</div>
    <ul class="sidebar-nav">
      <li>
        <a href="/admin/dashboard" class="<%= typeof activePage !== 'undefined' && activePage === 'dashboard' ? 'active' : '' %>">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
          Dashboard
        </a>
      </li>
      <li>
        <a href="/admin/properties" class="<%= typeof activePage !== 'undefined' && activePage === 'properties' ? 'active' : '' %>">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
          Properties
        </a>
      </li>
      <li>
        <a href="/admin/properties/new" class="<%= typeof activePage !== 'undefined' && activePage === 'new' ? 'active' : '' %>">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="16"/><line x1="8" y1="12" x2="16" y2="12"/></svg>
          Add Property
        </a>
      </li>
    </ul>
    <div class="sidebar-footer">
      <a href="/" style="color:rgba(255,255,255,0.5);font-size:0.82rem;display:block;margin-bottom:8px">← View Website</a>
      <a href="/admin/logout" class="btn btn-danger btn-sm w-full" style="justify-content:center">Logout</a>
    </div>
  </aside>

  <!-- Main content -->
  <main class="admin-main">
    <% if (typeof flashSuccess !== 'undefined' && flashSuccess) { %>
    <div class="flash flash-success">✓ <%= flashSuccess %></div>
    <% } %>
    <% if (typeof flashError !== 'undefined' && flashError) { %>
    <div class="flash flash-error">✕ <%= flashError %></div>
    <% } %>

    <%- body %>
  </main>
</div>
<script src="/js/main.js"></script>
<script src="/js/admin.js"></script>
</body>
</html>
